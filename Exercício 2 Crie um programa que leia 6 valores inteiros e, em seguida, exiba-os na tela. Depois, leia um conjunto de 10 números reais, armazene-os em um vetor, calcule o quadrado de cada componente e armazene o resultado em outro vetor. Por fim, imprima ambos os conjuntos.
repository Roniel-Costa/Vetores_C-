#include <iostream>
using namespace std;

int main() {
    int A[6];
   
    for ( int i = 0; i < 6; i++) {
        cout << "Digite um valor inteiro " << endl;
        cin >> A[i];
    }
   
   
    for (int i = 0; i < 6; i++) {
        cout << A[i] << endl;
        }
   
   
     float B[10], C[10];
   
    for ( int i = 0; i < 10; i++) {
        cout << "Digite um valor real " << endl;
        cin >> B[i];
       
        C[i] = B[i] * B[i];
    }
   
    for (int i = 0; i < 6; i++) {
        cout << "Valores do vetor A inteiro: " << A[i] << endl;
        }
       
       
    for (int i = 0; i < 10; i++) {
        cout << "Valores do vetor C real: " << C[i] << endl;
        }
       
    return 0;
    }
